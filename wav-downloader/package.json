{
  "name": "beatpad-workspace",
  "version": "2.0.0",
  "description": "BeatPad — Dockerized local songwriting workspace",
  "scripts": {
    "dev": "docker compose up --build",
    "down": "docker compose down",
    "logs": "docker compose logs -f",
    "reset": "docker compose down -v"
  }
}
